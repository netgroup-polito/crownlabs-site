apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: website
spec:
  rules:
    - host: preprod.crownlabs.polito.it
      http:
        paths:
          - backend:
              serviceName: website
              servicePort: 80
            path: /
  tls:
    - hosts:
        - preprod.crownlabs.polito.it
      secretName: website-cert
